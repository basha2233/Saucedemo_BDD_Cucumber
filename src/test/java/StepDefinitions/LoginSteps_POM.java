package StepDefinitions;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.edge.EdgeDriver;
import org.openqa.selenium.firefox.FirefoxDriver;

import Pages.loginPage;
import io.cucumber.java.en.And;
import io.cucumber.java.en.Given;
import io.cucumber.java.en.Then;
import io.cucumber.java.en.When;

public class LoginSteps_POM {/*
								 * 
								 * WebDriver driver = null; loginPage login;
								 * 
								 * @Given("user is on login page") public void user_is_on_login_page() { //
								 * Write code here that turns the phrase above into concrete actions
								 * System.out.println("login page"); System.out.println("browser open");
								 * System.getProperty("Webdriver.chrome.driver",
								 * "N:/Testing/Selenium2023June/Cucumber/src/test/resources/driver/chromedriver.exe"
								 * ); // driver = new ChromeDriver(); //driver = new EdgeDriver(); driver=new
								 * FirefoxDriver(); driver.navigate().to("https://www.saucedemo.com/");
								 * driver.manage().window().maximize();
								 * 
								 * }
								 * 
								 * @When("^user enters (.*) and (.*)$") public void
								 * user_enters_username_and_password(String username, String password) { try {
								 * // Write code here that turns the phrase above into concrete actions
								 * System.out.println("username and password"); login = new loginPage(driver);
								 * login.enterUsername(username); login.enterpassword(password); } catch
								 * (Exception e) { // TODO Auto-generated catch block e.printStackTrace(); } }
								 * 
								 * @And("clicks on login button") public void clicks_on_login_button() { //Write
								 * code here that turns the phrase above into concrete actions
								 * System.out.println("login button"); login.enterlogin(); }
								 * 
								 * @Then("user is navigated to home page") public void
								 * user_is_navigated_to_home_page() { // Write code here that turns the phrase
								 * above into concrete actions System.out.println("Homepage");
								 * login.products_display(); }
								 */
	  
	 }
